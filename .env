# 服务器端口（开发环境默认 3000，生产环境默认 8000）
PORT=3000

# 运行环境
NODE_ENV=development

# 管理员认证（必须设置，禁止使用默认值）
ADMIN_USER=admin
ADMIN_PASS=admin123
# JWT 签名密钥（必须设置，使用强随机字符串）
JWT_SECRET=change-me-in-production

# 生产环境 CORS 允许的来源
CORS_ORIGIN=https://yourdomain.com
