# 服务器端口（开发环境默认 3010，生产环境默认 8000）
PORT=3010

# 运行环境
NODE_ENV=development

# 管理员认证 - 生产环境必须修改默认值
ADMIN_USER=admin
ADMIN_PASS=admin123
# JWT 签名密钥 - 生产环境必须修改为强随机字符串（至少 32 字符）
JWT_SECRET=change-me-in-production

# 生产环境 CORS 允许的来源
CORS_ORIGIN=https://yourdomain.com
